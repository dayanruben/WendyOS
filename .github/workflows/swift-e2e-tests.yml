name: Swift E2E Tests

on:
  push:
    branches:
      - kb.swift-e2e-tests
  workflow_dispatch:
    inputs:
      tests:
        description: 'Comma-separated SwiftPM test filters (empty = WendyE2ETests)'
        required: false
        default: ''
  workflow_call:
    inputs:
      tests:
        type: string
        required: false
        default: ''

jobs:
  swift-e2e-tests:
    name: ${{ matrix.target.name }}
    # These entries are placeholders for the real device-to-device matrix. They
    # intentionally run on GitHub-hosted Ubuntu while every combination is marked
    # "Not Supported Yet" so the matrix is visible without requiring all planned
    # self-hosted runners and devices to exist.
    runs-on: ubuntu-latest
    timeout-minutes: 5
    strategy:
      fail-fast: false
      matrix:
        target:
          - id: macos-26__jetson-orin-nano
            name: macOS 26 → Jetson Orin Nano 🚧
            status: not-supported-yet
            status_label: Not Supported Yet
            runner_id: macos-26
            runner_name: macOS 26
            runner_labels: 'self-hosted, macOS, ARM64, macos-26'
            cli_os: macOS
            device_id: jetson-orin-nano
            device_name: Jetson Orin Nano
            transport: lan

          - id: ubuntu-24-04__jetson-orin-nano
            name: Ubuntu 24.04 → Jetson Orin Nano 🚧
            status: not-supported-yet
            status_label: Not Supported Yet
            runner_id: ubuntu-24-04
            runner_name: Ubuntu 24.04
            runner_labels: 'self-hosted, Linux, X64, ubuntu-24-04'
            cli_os: linux
            device_id: jetson-orin-nano
            device_name: Jetson Orin Nano
            transport: lan

          - id: windows-11__jetson-orin-nano
            name: Windows 11 → Jetson Orin Nano 🚧
            status: not-supported-yet
            status_label: Not Supported Yet
            runner_id: windows-11
            runner_name: Windows 11
            runner_labels: 'self-hosted, Windows, X64, windows-11'
            cli_os: windows
            device_id: jetson-orin-nano
            device_name: Jetson Orin Nano
            transport: lan

          - id: macos-26__raspberry-pi-5
            name: macOS 26 → Raspberry Pi 5 🚧
            status: not-supported-yet
            status_label: Not Supported Yet
            runner_id: macos-26
            runner_name: macOS 26
            runner_labels: 'self-hosted, macOS, ARM64, macos-26'
            cli_os: macOS
            device_id: raspberry-pi-5
            device_name: Raspberry Pi 5
            transport: lan

          - id: ubuntu-24-04__raspberry-pi-5
            name: Ubuntu 24.04 → Raspberry Pi 5 🚧
            status: not-supported-yet
            status_label: Not Supported Yet
            runner_id: ubuntu-24-04
            runner_name: Ubuntu 24.04
            runner_labels: 'self-hosted, Linux, X64, ubuntu-24-04'
            cli_os: linux
            device_id: raspberry-pi-5
            device_name: Raspberry Pi 5
            transport: lan

          - id: windows-11__raspberry-pi-5
            name: Windows 11 → Raspberry Pi 5 🚧
            status: not-supported-yet
            status_label: Not Supported Yet
            runner_id: windows-11
            runner_name: Windows 11
            runner_labels: 'self-hosted, Windows, X64, windows-11'
            cli_os: windows
            device_id: raspberry-pi-5
            device_name: Raspberry Pi 5
            transport: lan

          - id: macos-26__esp32-c6
            name: macOS 26 → ESP32-C6 🚧
            status: not-supported-yet
            status_label: Not Supported Yet
            runner_id: macos-26
            runner_name: macOS 26
            runner_labels: 'self-hosted, macOS, ARM64, macos-26'
            cli_os: macOS
            device_id: esp32-c6
            device_name: ESP32-C6
            transport: ble

          - id: ubuntu-24-04__esp32-c6
            name: Ubuntu 24.04 → ESP32-C6 🚧
            status: not-supported-yet
            status_label: Not Supported Yet
            runner_id: ubuntu-24-04
            runner_name: Ubuntu 24.04
            runner_labels: 'self-hosted, Linux, X64, ubuntu-24-04'
            cli_os: linux
            device_id: esp32-c6
            device_name: ESP32-C6
            transport: ble

          - id: windows-11__esp32-c6
            name: Windows 11 → ESP32-C6 🚧
            status: not-supported-yet
            status_label: Not Supported Yet
            runner_id: windows-11
            runner_name: Windows 11
            runner_labels: 'self-hosted, Windows, X64, windows-11'
            cli_os: windows
            device_id: esp32-c6
            device_name: ESP32-C6
            transport: ble
    steps:
      - name: Not Supported Yet
        shell: bash
        run: |
          set -euo pipefail
          {
            echo "## ${{ matrix.target.name }}"
            echo
            echo "Status: **${{ matrix.target.status_label }}**"
            echo
            echo "This matrix entry is intentionally visible but currently runs as a no-op."
            echo
            echo "| Field | Value |"
            echo "| --- | --- |"
            echo "| Runner | ${{ matrix.target.runner_name }} |"
            echo "| Planned runner labels | \`${{ matrix.target.runner_labels }}\` |"
            echo "| CLI OS | \`${{ matrix.target.cli_os }}\` |"
            echo "| Device | ${{ matrix.target.device_name }} |"
            echo "| Device ID | \`${{ matrix.target.device_id }}\` |"
            echo "| Transport | \`${{ matrix.target.transport }}\` |"
          } >> "$GITHUB_STEP_SUMMARY"
